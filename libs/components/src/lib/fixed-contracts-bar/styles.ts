import { Button, Row } from 'antd';
import styled from 'styled-components';

export const StyledFixedContractsBar = styled(Row)`
  width: 100%;
  position: sticky;
  top: 0px;
`;

export const StyledPage = styled(Button)`
  width: 100px;
`;
