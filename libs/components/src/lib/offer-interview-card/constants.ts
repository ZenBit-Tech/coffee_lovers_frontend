import { Job } from 'src/redux/types/jobs.types';
import { User } from 'src/redux/types/user.types';

export const avatarSize = 80;
export const idOfUndefined = 0;
export const userOfUndefined = { id: 0 } as User;
export const jobOfUndefined = { id: 0 } as Job;
